<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hx.dao.UserFileDao">
    <insert id="saveUserFile" parameterType="UserFile">
        insert into user_file(uid,finename,destpath) values (#{uid},#{fileName},#{destPath})
    </insert>
</mapper>